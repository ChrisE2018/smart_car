/*
 * Enum.hpp
 *
 *  Created on: Dec 26, 2022
 *      Author: cre
 */

#pragma once

enum Mode
{
    COMMAND_MODE = 0,
    DEMO_MODE,
    WALL_MODE,
    FORWARD_MODE,
    REVERSE_MODE,
    CLOCKWISE_MODE,
    COUNTERCLOCKWISE_MODE
};

